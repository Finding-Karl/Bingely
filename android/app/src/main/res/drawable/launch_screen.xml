<?xml version="1.0" encoding="utf-8"?>
<!-- Cold-start window background (see styles.xml's AppTheme) - shows
     briefly before the RN JS bundle mounts. Same red + "b" glyph as the
     app icon (src: splash_glyph.png, transparent PlayfairDisplay Italic
     "b" - see AppIcon generation notes in CHANGELOG.md) so the launch
     moment matches the icon instead of a plain white flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item
        android:width="180dp"
        android:height="180dp"
        android:gravity="center"
        android:drawable="@drawable/splash_glyph" />
</layer-list>
