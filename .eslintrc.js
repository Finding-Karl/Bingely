module.exports = {
  root: true,
  extends: '@react-native',
  ignorePatterns: [
    // Generated by `firebase deploy --only dataconnect` / the SQL Connect
    // VS Code extension. We call SQL Connect's GraphQL API directly instead
    // of using this generated client (see src/services/dataConnect.ts), so
    // it's unused boilerplate that shouldn't be linted.
    'src/dataconnect-generated/',
  ],
};
