module.exports = {
  root: true,
  extends: '@react-native',
  ignorePatterns: [
    // Leftovers from the abandoned SQL Connect approach that this sandbox
    // can't delete - see .gitignore for the full explanation.
    '_to_delete/',
  ],
};
